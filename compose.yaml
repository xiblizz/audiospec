services:
    audiospec:
        build: .
        container_name: audiospec
        ports:
            - '3000:3000'
        environment:
            - PORT=3000
            - NODE_ENV=development
        volumes:
            - .:/app
            - /app/node_modules
        restart: unless-stopped
